The nginx web server that is being used as a reverse proxy acts an an entry point for my web server, which handles the initial connection request and static files. For servers that are hosting Javascript and HTML files like mine, it is much faster at serving those files than the flask application itself is, which alleviates that burden from the flask application and allows it to only have to only have to deal with the code it needs to fulfil it's function.